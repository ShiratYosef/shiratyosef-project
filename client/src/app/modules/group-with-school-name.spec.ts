import { GroupWithSchoolName } from './group-with-school-name';

describe('GroupWithSchoolName', () => {
  it('should create an instance', () => {
    expect(new GroupWithSchoolName()).toBeTruthy();
  });
});
