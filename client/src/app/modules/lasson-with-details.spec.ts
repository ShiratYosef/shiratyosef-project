import { LassonWithDetails } from './lasson-with-details';

describe('LassonWithDetails', () => {
  it('should create an instance', () => {
    expect(new LassonWithDetails()).toBeTruthy();
  });
});
