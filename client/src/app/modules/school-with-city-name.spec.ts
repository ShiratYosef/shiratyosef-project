import { SchoolWithCityName } from './school-with-city-name';

describe('SchoolWithCityName', () => {
  it('should create an instance', () => {
    expect(new SchoolWithCityName()).toBeTruthy();
  });
});
